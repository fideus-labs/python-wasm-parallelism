# Phase 04: Browser Demo, Playwright Tests, and Browser Benchmarks

This phase brings the pool and the dask scheduler to their real target environment — the browser. It adds a Vite-served demo page (with COOP/COEP headers so SharedArrayBuffer is available), verifies everything works in a real Chromium via Playwright, and produces a browser benchmark report that mirrors the Node one. Consult the playwright-best-practices skill when writing the test suite.

## Tasks

- [x] Set up the browser demo app under `web/`. Search the existing codebase first and reuse `src/` as-is — the pool and worker were written environment-aware in Phase 01; only the entry wiring should be new:
  - `vite.config.ts` — root `web/`, dev-server and preview headers `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp` (required for SharedArrayBuffer), and worker bundling config so `src/worker/pyodide-worker.ts` is served as a module worker; in the browser, load Pyodide inside workers from the jsDelivr CDN (`indexURL`) instead of node_modules — make the worker's `loadPyodide` source configurable via the init/exec message
  - `web/index.html` + `web/main.ts` — a minimal but presentable page: status line (main Pyodide boot, pool warmup), buttons "Run serial", "Run parallel (4 workers)", "Run dask graph", a progress bar driven by the pool's progress callback, and a results table showing wall-clock times, speedup, and result-equality check; expose the app object as `window.__demo` with promise-returning methods so Playwright can drive it deterministically
  - npm scripts: `dev` (vite), `build:web`, `preview`. Verify `npm run build:web` succeeds

  > **Done 2026-07-17.** Worker/pool reused unchanged apart from the required knob: a new optional `PyodideSource { moduleURL, indexURL }` field on every protocol request (`exec`/`execPickled`/`ping`), set pool-wide via `PyodidePoolOptions.pyodideSource` — the worker dynamically imports `loadPyodide` from `moduleURL` (jsDelivr CDN in the browser) and falls back to the bare `pyodide` import (Node/node_modules). Covered by a new Node test (`tests/pool.test.ts`, file:// moduleURL); full suite 51/51 green. `web/main.ts` bundles the worker via Vite `?worker&url` (ES-format workers, `pyodide` external) and the `python/pyodide_pool` sources via `?raw`; dask installs lazily on first "Run dask graph" click. `window.__demo` exposes `ready`, `runSerial()`, `runParallel(workers?)`, `runDask()`, `runFailing()` (drives the UI error area), `results()`, `config`. Verified: `npm run build:web` clean, `npm run typecheck` (root + new `web/tsconfig.json`), preview AND dev servers send both COOP/COEP headers (curl-checked). Pyodide CDN pinned to the installed 314.0.2.

- [x] Manually smoke-test the page headlessly before writing formal tests: launch `npm run dev`, drive it with the Playwright MCP browser tools (navigate, click "Run parallel", read the results table, capture console errors), and fix what's broken — typical issues: worker module resolution under Vite, `crossOriginIsolated` false due to missing headers on worker/CDN responses (pyodide CDN assets need `crossorigin` handling under COEP; if `require-corp` blocks CDN loading, switch to `Cross-Origin-Embedder-Policy: credentialless` and document the trade-off), and `import.meta.url` worker URL differences between dev and build.

  > **Done 2026-07-17 — everything passed; nothing needed fixing.** Tooling note first: this host is **Linux ARM64** (WSL2), so the Playwright MCP server (hard-configured for the `chrome` channel at `/opt/google/chrome`) and the chrome-devtools MCP both fail to launch, and no passwordless sudo exists for a system Chrome. Drove the page instead with the **agent-browser CLI** pointed at Playwright's ARM64 Chromium (`npx playwright install chromium` → `~/.cache/ms-playwright/chromium-1228/chrome-linux/chrome`) — same headless-CDP smoke, and it confirms `playwright install chromium` + launch works here **without** `--with-deps`/sudo (relevant to the next task). Results, dev server (`npm run dev`, :5173): COOP/COEP headers present, `crossOriginIsolated === true`; main Pyodide + 4-worker pool boot from jsDelivr under `require-corp` with no COEP blocks (no `credentialless` fallback needed); serial 6.31 s → parallel (4 w) 1.99 s, **3.17×**, per-chunk counts identical, progress 8/8; dask button installs dask from PyPI, pool 2.16 s vs sync 6.34 s (**2.93×**), totals equal & correct; `runFailing()` renders the full Python traceback in the UI error area with no unhandled rejection; console clean (only Vite-client debug + Pyodide package-load logs — those two `Loading/Loaded click, fsspec, packaging, pyyaml, toolz` log lines are the allow-list candidates for the e2e console fixture). Also smoke-tested the production bundle via `vite preview` (:4173): headers OK, isolated true, parallel run correct (1.93 s), **zero** console messages — no dev-vs-build worker-URL drift. Screenshot: `Working/smoke-dev-final.png`.

- [x] Set up the Playwright test project: `npx playwright install chromium --with-deps` (if `--with-deps` needs sudo and fails, run plain `install` and verify launch works), `playwright.config.ts` with a `webServer` entry that runs `npm run preview` against the built site (test the production bundle, not the dev server), `baseURL`, trace-on-first-retry, and a long `expect` timeout budget for Pyodide boots. Add npm script `test:browser`.

  > **Done 2026-07-17.** `--with-deps` failed on sudo exactly as predicted, plain `npx playwright install chromium` sufficed (build 1228 / Chromium 149.0.7827.0 already cached from the smoke-test task) and a headless launch+navigate was verified. Added `@playwright/test` devDependency and `playwright.config.ts`: testDir `e2e/`, single chromium project (Desktop Chrome), `webServer` = `npm run build:web && npm run preview -- --strictPort` at `http://localhost:4173` so every run tests a **freshly built production bundle** (`--strictPort` fails loudly if 4173 is squatted; `reuseExistingServer` locally), `baseURL`, `trace: 'on-first-retry'` (retries CI-only), test timeout 240 s / `expect` timeout 120 s for CDN Pyodide boots with tight action (15 s)/navigation (30 s) timeouts, and `workers: 1` + `fullyParallel: false` (each page boots main-Pyodide + a worker pool; parallel files would contend for CPU and skew `@bench` timings). npm script `test:browser` = `playwright test --grep-invert @bench` — the @bench exclusion lives in the **script, not config**, because a config-level `grepInvert` combines with a CLI `--grep @bench` and would match nothing. Verified end-to-end with a throwaway spec (deleted after): webServer built + served, page cross-origin isolated headless, a deliberately-failing `@bench` test was excluded from `npm run test:browser` (1/2 tests ran) and selected by `--grep @bench --list`. Root tsconfig now includes `e2e` + `playwright.config.ts` (`npm run typecheck` green); `playwright-report/` gitignored. Note for the next task: `e2e/` is empty, so `npm run test:browser` says "no tests found" until the suite lands.

- [x] Write the browser test suite in `e2e/`:
  - `e2e/pool.spec.ts` — page reports `crossOriginIsolated === true`; pool warmup completes; parallel prime-count run returns counts identical to the serial run; a Python exception in a worker surfaces as a readable error in the UI, not an unhandled rejection; progress bar reaches 100%
  - `e2e/dask.spec.ts` — the dask graph button computes the delayed reduction in-browser and matches the synchronous-scheduler result; a numpy task exercises package mirroring from the CDN
  - Assert zero unexpected console errors across all specs (collect via a shared fixture; allow-list known benign Pyodide messages)
  - `e2e/bench.spec.ts` — tagged `@bench` and excluded from the default run: executes the serial and 2/4-worker parallel workloads via `window.__demo`, 3 repetitions, and writes `bench/results/browser-<iso-date>.json` in the same schema as the Node results

  > **Done 2026-07-17.** Suite = `e2e/fixtures.ts` + 3 specs. The fixture is an `{ auto: true }` watchdog on every test: collects `console.error` + `pageerror` (the latter fires for unhandled rejections, which is what makes the "readable error, not an unhandled rejection" assertion real), asserts empty at teardown; allow-list = Pyodide's `Loading …`/`Loaded …` package-mirroring lines flagged by the smoke test. All assertions structural (result equality, `8/8` progress, ✓ cells) — no duration asserts. Two demo additions were needed and made: `window.__demo.runNumpy()` (dask graph whose leaves `import numpy` — driver loads numpy from the CDN, workers mirror it; the `tests/dask-scheduler.test.ts:331` topology) and `__demo.setup()` (per-pool `warmup()` wall/boot times + runtime version, so the bench JSON gets honest `setup[]`/`workerBoot` numbers). `__demo` types moved to Vite-free `web/demo-api.ts` so the root (NodeNext) tsconfig can share them with `e2e/`; `<progress>` asserted via live `value === max` (it's property-driven — attributes never update). `@bench` uses the `{ tag }` API + `test.setTimeout(600_000)`; writes `browser-<date>.json` conforming to `BenchResults` — real `setup[]` (pool 4 boots at init, pool 2 on first use; boots ~1.46 s each), unmeasured noop/payload overheads use run-bench's own `{samples: [], medianMs: NaN}` convention, serial recorded as poolSize 1. Schema tweaks: optional `BenchContext.browser/userAgent`, per-workload configs optional with `NodeBenchConfig = Required<BenchConfig>` for the Node harness (generator reads none of the optionals). Verified: `npm run test:browser` 6/6 green (31.6 s; also each spec file green standalone), `@bench` 1/1 green (47.3 s) → `bench/results/browser-2026-07-17.json` (serial 5.82 s / 2w 3.30 s / 4w 1.85 s, all cells verify 148 933, `failures: []`), `npm run typecheck` clean, Node vitest 51/51.

- [x] Run `npm run test:browser` and fix failures until the suite is green twice consecutively. Timing-sensitive assertions must be structural (results equal, progress complete) — never assert absolute durations in tests; speedup numbers belong in the benchmark report only.

  > **Done 2026-07-17 — green twice consecutively on the first attempt; zero fixes needed.** Run 1: 6/6 passed in 31.3 s; run 2: 6/6 passed in 31.9 s (per-test times stable across runs, e.g. dask graph 11.5 s both times — each run rebuilds the production bundle via the `webServer` command, so both were fresh-bundle runs). Also re-audited the whole suite for the timing rule: `grep -nE 'toBeLessThan|toBeGreaterThan|durationMs|elapsed|speedup' e2e/*.ts` matches only the doc comment in `pool.spec.ts` stating the policy — every assertion in `pool.spec.ts`/`dask.spec.ts`/`fixtures.ts` is structural (result equality, `8/8` progress with live `value === max`, ✓ equality cells, `status === ready`, empty console-error watchdog), and `bench.spec.ts` records durations into the results JSON without asserting on them. No code changes; nothing to fix.

- [ ] Run the `@bench` spec (`npx playwright test --grep @bench`) and generate `docs/benchmarks/browser-benchmarks.md` from the results JSON, reusing the report-generation code from `bench/` (refactor it into a shared module rather than copying): front matter (`type: report`, tags `[benchmark, browser, playwright]`), wiki-links `[[node-benchmarks]]` and `[[phase-02-results]]`, the same table layout as the Node report plus a Node-vs-browser comparison section noting CDN package-load costs, and update `[[node-benchmarks]]`'s forward link if the filename differs. Record any browser-specific workarounds in `docs/notes/phase-04-results.md` (front matter `type: note`).

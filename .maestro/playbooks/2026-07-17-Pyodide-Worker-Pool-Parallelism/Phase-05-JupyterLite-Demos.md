# Phase 05: JupyterLite Demos

This phase packages the driver-side Python API as a wheel, embeds the JS worker-pool glue so it can be loaded from inside JupyterLite's Pyodide kernel (which itself runs in a Web Worker — the pool spawns nested workers), and ships a JupyterLite site with demo notebooks: pool basics, parallel dask computation, and an in-notebook benchmark. The deliverable is a locally served JupyterLite where a user runs `await compute(...)` in a notebook and watches it parallelize.

## Tasks

- [x] Set up the Python tooling and scaffold the JupyterLite site under `demos/jupyterlite/`. Create a virtualenv (`uv venv && uv pip install ...` if `uv` is available, else `python3 -m venv .venv`) with `jupyterlite-core` and `jupyterlite-pyodide-kernel`. Create `demos/jupyterlite/jupyter_lite_config.json` and a `demos/jupyterlite/files/` content directory, plus npm scripts `build:lite` (runs `jupyter lite build` with output to `demos/jupyterlite/_output`) and `serve:lite` — a small Node script `scripts/serve-lite.mjs` that serves `_output` with COOP/COEP headers (SharedArrayBuffer and nested-worker isolation; reuse the header values from `vite.config.ts`).
  > Done 2026-07-17: `.venv` (uv, CPython 3.13) with `jupyterlite-core 0.8.1`, `jupyterlite-pyodide-kernel 0.8.2`, plus `jupyter-server` (required by the contents addon — build fails without it). `build:lite` cd's into `demos/jupyterlite` before running `jupyter lite build` because jupyterlite resolves `--output-dir` and its `.jupyterlite.doit.db` cache against CWD, not `--lite-dir`. `scripts/serve-lite.mjs` serves `_output` on port 8000 (or `--port`/`PORT`) with the same COOP/COEP values as `vite.config.ts`; verified headers, MIME types, and 404 via curl. 51/51 vitest tests still pass.

- [x] Make the JS pool loadable from inside the kernel worker, and build the Python wheel:
  - Add an esbuild target that bundles `src/index.ts` + the worker URL wiring into a single self-contained ESM file `dist/pyodide-pool.browser.js` (worker code inlined as a Blob URL or emitted as a sibling asset — nested `new Worker()` inside the kernel worker must resolve without Vite); copy it into `demos/jupyterlite/files/assets/` during `build:lite`
  - Give `python/pyodide_pool` a `pyproject.toml` (hatchling or flit, pure-Python wheel) and add `loader.py`: `async def create_pool(pool_size=4, js_url=None)` that dynamically imports the JS bundle from Python via `pyodide.code.run_js("import('<url>')")`, constructs the JS `PyodidePool`, and returns the Python `WorkerPool` wrapper — so notebooks never touch JS directly
  - Build the wheel (`python -m build` or `uv build`) into `demos/jupyterlite/files/wheels/` and verify with a quick Node check that the wheel installs via micropip and imports cleanly in a bare Pyodide
  > Done 2026-07-17: `src/browser.ts` + shared `scripts/bundles.mjs` build `dist/pyodide-pool.browser.js` (26.8kb, worker-pool inlined, worker source embedded via esbuild `define` and spawned from a Blob URL; only dynamic-import externals `pyodide`/`web-worker` remain — dead paths in browsers). `createPool()` defaults pyodideSource to the jsDelivr CDN pinned to the installed pyodide version (314.0.2). `build:lite` now runs `scripts/build-lite-assets.mjs` first: copies the bundle to `files/assets/` and `uv build`s the wheel to `files/wheels/` (both gitignored; uv's stray `.gitignore` in --out-dir is removed). `python/pyproject.toml` (hatchling, dynamic version from `__init__`, dep `cloudpickle`, extra `dask`); `loader.py:create_pool` imports the bundle via `run_js("import('<url>')")` (default `/files/assets/pyodide-pool.browser.js`), wraps it in `WorkerPool`, and installs it via new `_bridge.set_default_pool` so bare `pyodide_pool.compute(...)` works in notebooks; re-exported lazily as `pyodide_pool.create_pool`. Verified: `npm run check:wheel` (bare-Pyodide micropip install of the wheel from emfs + import + loader check — 9/9 green), new vitest suites `tests/browser-bundle.test.ts` + `tests/loader.test.ts` (the loader drives the real bundle through `run_js` in Node — works on Node 24; JsProxy identity must compare `js_id`, never `is`), full suite 62/62, typecheck clean, `build:lite` end-to-end puts both artifacts in `_output/files/`.

- [x] Author the demo notebooks in `demos/jupyterlite/files/` (JupyterLite's Pyodide kernel supports top-level `await` — use it throughout; first cell of each notebook: `import piplite; await piplite.install('wheels/pyodide_pool-<version>-py3-none-any.whl')` — use `%pip` if piplite is aliased):
  - `01-pool-basics.ipynb` — create a 4-worker pool, `await pool.submit(...)` a CPU-bound prime count, then map 8 chunks and compare wall-clock vs a serial loop with a simple matplotlib bar chart of the timings
  - `02-dask-parallel.ipynb` — the Phase 02 scheduler in a notebook: build a `dask.delayed` graph and a `dask.bag` pipeline, `await compute(...)` on the pool, verify against `scheduler='synchronous'`, and demonstrate package mirroring with a numpy task
  - `03-benchmark.ipynb` — parameterized benchmark (workers × workload) rendering a results table and speedup chart; keep default parameters small enough to finish in ~1 minute in-browser
  - Markdown cells in each notebook must explain what is happening and note the environment requirements (cross-origin isolation, nested worker support)
  > Done 2026-07-17: all three notebooks authored (nbformat 4.5, kernelspec `python`/"Python (Pyodide)" — matches what the built pyodide-kernel-extension registers). One deliberate deviation: the install cell uses the ABSOLUTE path `/files/wheels/pyodide_pool-0.1.0-py3-none-any.whl` — the kernel worker resolves relative URLs against its own script URL (under `extensions/.../static/`), so the spec's `wheels/...` form would 404; absolute matches `loader.DEFAULT_JS_URL`'s root-hosting assumption. 01 uses the repo-standard π(2,000,000)=148,933 across 8 chunks (serial loop in the kernel vs `asyncio.gather` of `pool.submit`, warm-pool timing, matplotlib bar chart) and ends with `assert parallel < serial` + `print("POOL_DEMO_OK", speedup)` — the stable marker the Phase-05 smoke test will assert. 02 uses π(1,000,000)=78,498 (keeps the two `scheduler='synchronous'` verification runs quick): `dask.delayed` graph + `dask.bag` pipeline + numpy-mirroring graph with an exact integer-sum check; ends `DASK_DEMO_OK`. 03: `WORKER_COUNTS=[1,2,4] × WORKLOADS={500k,1M}`, `REPS=1`, one warmed pool per worker count (terminated after its column), median timing, `IPython.display.Markdown` results table + speedup line chart (validated dataviz palette); ends `BENCH_DEMO_OK`; ~15–25 s total in-browser. Verified: `nbformat.validate` + every code cell compiles under `PyCF_ALLOW_TOP_LEVEL_AWAIT`, `build:lite` ingests all three into `_output/files/`, vitest 62/62. Generator kept at `Working/generate-notebooks.py`; `files/README.md` updated.

- [ ] Build and verify the site end-to-end: run `npm run build:lite`, start `npm run serve:lite`, and drive it with the Playwright MCP browser tools — open `01-pool-basics.ipynb` in JupyterLite, run all cells, and confirm the parallel timing beats serial and no cell errors; then run `02-dask-parallel.ipynb` the same way. Fix whatever breaks (likely candidates: nested-worker spawning inside the kernel worker, asset paths under the JupyterLite base URL, wheel filename drift in the first cell, COEP blocking the pyodide CDN inside nested workers — fall back to `credentialless` or self-host pyodide assets in `files/assets/pyodide/` if needed).

- [ ] Add a permanent Playwright smoke test `e2e/jupyterlite.spec.ts` to the existing config as a separate project with its own `webServer` entry (`npm run serve:lite`): opens `01-pool-basics.ipynb`, runs all cells via the JupyterLab UI (Run menu → Run All Cells), waits for the final output, asserts the success marker the notebook's last cell prints (add an explicit `print("POOL_DEMO_OK", speedup)` cell for a stable assertion target) and zero unexpected console errors. Run it until green, then write `docs/notes/phase-05-results.md` (front matter `type: note`, tags `[jupyterlite, demo]`, wiki-links `[[phase-02-results]]`, `[[browser-benchmarks]]`) documenting the JupyterLite integration details and any workarounds.

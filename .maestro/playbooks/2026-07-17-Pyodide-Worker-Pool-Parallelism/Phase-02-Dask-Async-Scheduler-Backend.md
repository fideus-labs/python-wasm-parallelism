# Phase 02: Dask Async Scheduler Backend with Package Mirroring

This phase builds the headline feature: a dask scheduler that runs inside a "main" Pyodide instance and executes dask task graphs in parallel by dispatching work to the Pyodide worker pool from Phase 01. Callables and arguments travel to workers as cloudpickle payloads, and workers automatically mirror the packages installed in the main instance so user code (numpy, etc.) just works. The result is `await pyodide_pool.compute(delayed_obj)` producing the same answers as dask's synchronous scheduler, faster.

## Tasks

- [x] Review existing code and research docs, then write the design document before implementing. Read `src/pool/pyodide-pool.ts`, `src/worker/pyodide-worker.ts`, `docs/research/dask-schedulers.md`, and `docs/notes/phase-01-results.md`. Create `docs/architecture/dask-scheduler-design.md` with front matter (`type: analysis`, tags `[dask, architecture, scheduler]`) and wiki-links to `[[dask-schedulers]]` and `[[worker-pool-api]]`, covering:
  - The main-instance topology: a "driver" Pyodide runs user code + the scheduler; the JS `PyodidePool` is exposed to driver Python via `pyodide.registerJsModule('js_pyodide_pool', ...)`
  - Task wire format: `cloudpickle.dumps((func, args, kwargs))` → bytes → transferred to worker → unpickled, executed, result cloudpickled back (cloudpickle ships in the Pyodide distribution)
  - Scheduler algorithm: async graph executor — compute in-degrees over the dask graph (`dsk` dict), dispatch all ready tasks concurrently, on each completion decrement dependents and dispatch newly-ready tasks, until requested keys resolve; failures propagate the original Python traceback
  - Why the scheduler must be async (no blocking on the browser main thread) and how JS promises bridge to asyncio futures on Pyodide's event loop

  > **Done 2026-07-17.** `docs/architecture/dask-scheduler-design.md` created after reading all four sources. Covers the four required areas plus: the `execPickled` message shape and idempotent installed-set, `runPickled`/`mapPickled` signatures, the `python/pyodide_pool/` package layout (no top-level dask import, for Phase 06 reuse), package-mirroring snapshot/filter rules (`register_pickle_by_value(pyodide_pool)`), concurrent.futures-style remote-traceback re-raising, and a risks section (per-task ms overhead, no locality, no eager cache release, modern task-spec graphs needing dask on workers). Notable design decisions recorded: scheduler dispatches all ready tasks and lets the JS pool bound concurrency; workers never need dask for legacy-tuple graphs because scheduler helpers travel by value; protocol tasks still always resolve (worker-pool 1.0.0 rejection semantics from Phase 01).

- [x] Extend the worker protocol in `src/worker/pyodide-worker.ts` and rebuild:
  - New message `kind: 'execPickled'` carrying `{ id, payload: ArrayBuffer, packages: string[], wheels: string[] }` — ensure listed packages are loaded (`pyodide.loadPackage` for distribution packages, `micropip.install` for wheel URLs/PyPI names; keep a module-scope set of already-installed names so replays are idempotent), then unpickle with cloudpickle, call `func(*args, **kwargs)`, cloudpickle the result, and post back `{ id, ok: true, payload: ArrayBuffer }` using transferables
  - Preserve full Python tracebacks in the error path (send the formatted traceback string; also cloudpickle the exception object when possible so the driver can re-raise it)
  - Add matching methods on `PyodidePool` in `src/pool/pyodide-pool.ts`: `runPickled(payload, { packages, wheels })` and `mapPickled(payloads, opts)` built on `pool.runTasks` so progress reporting and `cancel(runId)` keep working. Run `npm run build && npm run typecheck`

  > **Done 2026-07-17.** `ExecPickledRequest`/`ExecPickledSuccess`/`ExecPickledResponse` added to the worker, `runPickled`/`mapPickled` + `RunPickledOptions`/`MapPickledOptions` to the pool, all re-exported from `src/index.ts`. Implementation notes: execution goes through a fixed Python shim (`RUN_PICKLED_SOURCE`, defined once per interpreter) that catches `BaseException`, formats the traceback, and cloudpickles the exception into `error.exceptionPayload` (also surfaced on `PyodideTaskError.exceptionPayload`); result/exception bytes are built as JS `Uint8Array`s Python-side via `JsBuffer.assign` so their buffers are plain transferable ArrayBuffers (payloads move zero-copy in both directions — the `web-worker` polyfill forwards transfer lists to `worker_threads`, verified in its source). Distribution packages reuse the existing `ensurePackages` (micropip fallback included, `pyodide.loadedPackages`-idempotent); wheels get a new module-scope `installedWheels` set since `loadedPackages` keys are names, not URLs. `PyodidePool.exchange` was generalized to be generic over the response union with an optional transfer list; pickled tasks keep the Phase-01 always-resolve contract so a failed task never shrinks the pool. `npm run build` + `npm run typecheck` clean; 8 new integration tests (4 worker-protocol, 4 pool-level: args/kwargs, lambda-by-value, traceback + pickled-exception re-raise, malformed request, ordered mapPickled with progress, empty-list short-circuit) — 31/31 pass.

- [x] Create the driver-side Python package under `python/pyodide_pool/` (this code runs in the MAIN Pyodide instance, not in workers):
  - `__init__.py` exporting the public API: `WorkerPool` (Python wrapper), `compute`, `get`
  - `_bridge.py` — wraps the JS pool object from `js_pyodide_pool`: `async def submit(func, /, *args, **kwargs)` that cloudpickles the call, converts to a JS-transferable buffer via `pyodide.ffi`, awaits the JS promise (JS promises are directly awaitable in Pyodide), and unpickles the returned buffer; raise the remote exception with its original traceback attached
  - `_packages.py` — package-mirroring helpers (see next task)
  - Keep the package pure-Python with no hard dask import at module top level so it can also serve the Phase 06 multiprocessing shim

  > **Done 2026-07-17.** `python/pyodide_pool/{__init__,_bridge,_packages}.py` created. `__init__.py` exports `WorkerPool`/`submit`/`default_pool`/`snapshot_packages` plus error types, resolves `compute`/`get` lazily from `.scheduler` via module-level `__getattr__` (so dask never loads at import time and the export works as soon as the next task creates `scheduler.py`), and calls `cloudpickle.register_pickle_by_value(pyodide_pool)` at import — covering all submodules, so payload-embedded helpers travel by value. `_bridge.submit` cloudpickles the call triple, builds a plain transferable ArrayBuffer via `Uint8Array.new(...).assign(...)`, passes the per-call package snapshot as `to_js({...}, dict_converter=Object.fromEntries)`, and awaits `runPickled` directly. Error path exploits that Pyodide's `JsException` IS a JsProxy of the JS error: `pythonTraceback`/`exceptionPayload` read straight off the caught exception; a picklable remote exception re-raises as the original object `from RemoteTraceback(tb)` (concurrent.futures style), unpicklable ones degrade to `RemoteExecutionError` with `.remote_traceback`. `_packages.snapshot_packages()` merges JS `pyodide.loadedPackages` with `micropip.list()`, splits distribution vs wheel targets, and dedupes/excludes by PEP 503 canonical name (micropip normalizes `fake_pkg` → `fake-pkg` — caught by a test; sources `"default channel"`/`"pyodide"` both count as distribution). Excluded from mirror: `pyodide_pool`, `cloudpickle`, `micropip`. New `tests/pyodide-pool-python.test.ts` boots a real driver Pyodide + 2-worker pool (the design-doc topology) and covers: API surface + by-value registry, submit with args/kwargs, `WorkerPool` wrapper + lambda-by-value, original-exception re-raise with `RemoteTraceback` cause carrying the remote frames, unpicklable-exception fallback, pool survival after failures, and snapshot split/filtering — 38/38 tests pass, `npm run build` + `typecheck` clean.

- [x] Implement the async dask scheduler in `python/pyodide_pool/scheduler.py`:
  - `async def get(dsk, keys, **kwargs)` implementing the design-doc algorithm on the raw graph (use `dask.core.get_dependencies` / `dask.core.toposort` helpers rather than hand-rolling graph utilities; handle nested key lists like dask's own `get`). Literal (non-task) graph values resolve locally without a worker round-trip
  - `async def compute(*collections, **kwargs)` that calls `dask.base.collections_to_dsk` / `obj.__dask_graph__()` + `__dask_keys__()` + `__dask_postcompute__()` so `dask.delayed`, `dask.bag`, and dask arrays all work
  - Dispatch each runnable task through `_bridge.submit`, resolving already-computed dependency values into the task's arguments before pickling (workers are stateless between tasks — ship values, don't assume a shared cache)
  - Support a `num_workers`-independent design: concurrency is bounded by the JS pool size, the scheduler just dispatches everything that is ready

  > **Done 2026-07-17.** `python/pyodide_pool/scheduler.py` created (Kahn-style async in-degree executor per the design doc: dispatch every ready task, `asyncio.wait(FIRST_COMPLETED)`, decrement dependents, fail-fast with cancel + `gather(return_exceptions=True)` settle; `num_workers` accepted-and-ignored; nested key lists → tuples exactly like `dask.get`; `toposort` cycle check; requested keys validated upfront; `pool=` kwarg for explicit `WorkerPool` wiring). **Empirical findings that shaped the code** (probed real dask 2026.7.1 in a driver Pyodide before implementing — probe kept at `Working/probe-dask.mjs`): (1) dask is **NOT** in the Pyodide v314 distribution — the design doc was right, the Phase-02 demo task's "(it is in the Pyodide distribution)" is wrong; the driver needs `micropip.install("dask")` (pulls partd/locket from PyPI, click/fsspec/packaging/pyyaml/toolz from the distribution). (2) `dask.base.collections_to_dsk` and `dask.core.nested_get` **no longer exist** — `compute` follows modern `dask.base.compute` (`collections_to_expr` + `FinalizeCompute` + `expr.optimize()`, finalization compiled into the graph, flat keys, `repack(results)`), with the classic `collections_to_dsk`/`__dask_postcompute__` path kept as an `ImportError` fallback for older dask; design doc update deferred to the results task per its "update if diverged" clause. (3) `dask.delayed`/`dask.bag` emit **modern `dask._task_spec` nodes**, and `istask(Alias)` is True — so classification checks `GraphNode` before `istask`: `Alias`/`DataNode` resolve locally via `node(values)`, real `Task`s go remote as `submit(node, values)` (callable-with-mapping convention, pickle by reference; workers get dask via mirroring — worker ordering verified: packages → wheels → unpickle). Legacy tuple tasks go remote as `submit(_evaluate_node, node, values)` where the recursive evaluator travels by value, keeping workers dask-free for that dialect. `compute` returns the bare result for one argument (matches `obj.compute()`), a tuple for several. New `tests/pyodide-scheduler.test.ts` (real driver + real PyPI dask + 2 warmed workers): raw legacy graph vs `dask.get` with a submit-count monkeypatch proving literals/alias/container stayed local (2 submits for a 5-node graph), delayed tree vs sync scheduler + multi-arg tuple + non-dask passthrough + `pool=` kwarg, concurrency proven via overlapping worker-side `(start, end)` intervals, original `ValueError` re-raise with `RemoteTraceback` cause + pool survival, and a `dask.bag` reduction (modern `Alias` path). 43/43 tests, `npm run build` + `typecheck` clean.

- [x] Implement package mirroring in `python/pyodide_pool/_packages.py` and the JS side:
  - Driver snapshot: read `pyodide.loadedPackages` (JS side) plus `micropip.list()` (driver Python) to build the set of packages/wheels the main instance has installed; expose `snapshot_packages()` and pass the snapshot with every `execPickled` message (the worker's installed-set makes replay cheap and idempotent — verify this rather than adding separate sync machinery)
  - Filter out packages that cannot or need not be mirrored (e.g. `pyodide_pool` itself is shipped via cloudpickle-by-value; enable `cloudpickle` register-by-value for the `pyodide_pool` module or keep worker-executed closures self-contained)
  - Verify with numpy: driver loads numpy, submits a lambda using numpy, worker installs numpy automatically and returns the correct array result

  > **Done 2026-07-17.** The mirroring machinery itself already landed with the two preceding tasks (driver snapshot in `_packages.snapshot_packages()`, riding on every `_bridge.WorkerPool.submit`; worker idempotency via `loadedPackages`-filtered `ensurePackages` plus the module-scope `installedWheels` set; filtering via `EXCLUDED_FROM_MIRROR` + `register_pickle_by_value(pyodide_pool)` — the latter two already unit-tested), so this task delivered the verification the bullets call for, adding **no** new sync machinery per the "verify rather than add" clause. New end-to-end test in `tests/pyodide-pool-python.test.ts` ("mirrors numpy to workers automatically; snapshot replay is idempotent"): a dedicated 1-worker pool (so both submits deterministically hit the same worker) is warmup-pinged to prove numpy is absent; the driver then loads numpy and submits a lambda closing over the `np` module — cloudpickle ships module refs by reference, so worker-side unpickling *requires* the mirrored install (which `handleExecPickled` runs before unpickling); the returned ndarray equals the locally computed array. Worker-side proof: a post-submit warmup ping shows numpy in the WORKER's own `pyodide.loadedPackages`. Idempotent replay proved two ways: a `_bridge.snapshot_packages` monkeypatch-counter shows a fresh snapshot rode on each of the 2 execPickled messages, and the second submit's wall-clock came in under half the first's (install skipped). The wheels/micropip mirror path is exercised continuously by `tests/pyodide-scheduler.test.ts` (PyPI dask mirrored to workers). 44/44 tests pass, `npm run typecheck` clean.

- [x] Create the Node demo `examples/node-dask-demo.ts` plus embedded Python driver script:
  - Boot a main Pyodide instance in the Node main thread with `loadPyodide()`, load `dask` (it is in the Pyodide distribution) and `cloudpickle`, register the JS pool via `registerJsModule`, and install `python/pyodide_pool` into the driver (write the package files into Pyodide's FS or install from a locally built wheel)
  - Driver script: build a `dask.delayed` graph (e.g. 8 CPU-heavy leaf tasks — the Phase 01 prime counter — feeding a `sum` reduction), run once with `dask`'s synchronous scheduler and once with `await pyodide_pool.compute(...)` on a 4-worker pool, assert identical results, print both wall-clock times and the speedup
  - Add a second mini-demo in the same script: `dask.bag.from_sequence(...).map(...).sum()` and a numpy-using delayed task to exercise package mirroring
  - Wire as `npm run demo:dask`

  > **Done 2026-07-17.** `examples/node-dask-demo.ts` created (structure mirrors `examples/node-demo.ts`; driver-boot pattern reuses `tests/pyodide-scheduler.test.ts`) and wired as `npm run demo:dask`. One deviation from the bullet text, matching the scheduler task's empirical finding: dask is **not** in the Pyodide v314 distribution, so the driver installs it with `micropip.install("dask")` from PyPI; `pyodide_pool` is installed by writing the package files into the driver FS (`/driver-site`). Three demos, each asserting pool == synchronous-scheduler results: (1) `dask.delayed` — 8 prime-count chunks over [2, 2 000 000) feeding `sum`, checked against π(2·10⁶) = 148933, wall-clocks + speedup printed; (2) `dask.bag.from_sequence(range(24)).map(busy_square).sum()`; (3) numpy mirroring — a batch of `POOL_SIZE` numpy `delayed` tasks (module ref pickled by reference, so unpickling *requires* the mirrored install) run twice, printing first-batch (auto-install) vs second-batch (cached) timings, values checked against driver-local numpy. Setup warms all 4 workers with concurrent submits so dask mirroring cost is paid up front and printed separately. Exits nonzero on any mismatch; warns (doesn't fail) below 1.5x speedup. Side fix while smoke-testing: worker `loadPyodide()` now routes interpreter stdout/stderr through `console.log`/`console.error` — Node worker_threads have no `process.stdout.fd`, so Pyodide's default stdout device threw a `TypeError` on every Python-level write (micropip's internal "Loading ..." messages during mirroring). Smoke run: exit 0, all three demos match, 2.95x speedup, numpy first batch 413 ms vs 44 ms cached; formal measurement + results doc belong to the next task. 44/44 tests, `npm run build` + `typecheck` clean.

- [ ] Run `npm run demo:dask` and iterate until it exits 0 with matching results and >1.5x speedup on the delayed graph. Then record `docs/notes/phase-02-results.md` (front matter `type: note`, tags `[dask, results]`, wiki-links `[[dask-scheduler-design]]`, `[[phase-01-results]]`) with measured timings, the package-mirroring behavior observed (first-task numpy install cost vs subsequent tasks), and any deviations from the design doc — update the design doc if the implementation diverged.

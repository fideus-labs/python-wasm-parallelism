# Phase 03: Node.js Test Suite and Benchmarks

This phase hardens the Phase 01–02 code with a Vitest suite covering the pool, the worker protocol, the dask scheduler, and package mirroring, then adds a repeatable benchmark harness that quantifies serial-vs-parallel performance across worker counts and workload types, publishing results as a structured markdown report.

## Tasks

- [x] Set up Vitest and write the pool/worker test suite. First read the existing source (`src/pool/pyodide-pool.ts`, `src/worker/pyodide-worker.ts`) and reuse the demo's setup helpers rather than duplicating boot code — extract shared test fixtures into `tests/helpers.ts` (e.g. a `withPool(size, fn)` helper that guarantees `terminate()`). Configure `vitest.config.ts` with a generous `testTimeout` (Pyodide boots take seconds) and a `test` npm script. Create `tests/pool.test.ts`:
  - `runPython` returns correct scalar, list, and dict results
  - Python exceptions propagate with the Python traceback text in the error
  - `map` over N > poolSize items returns ordered, correct results
  - Worker recycling: run two sequential tasks on a size-1 pool and assert the second task's reported `bootMs` is ~0 (interpreter reused, not re-booted)
  - Progress callback fires for each completed task; `cancel(runId)` rejects the batch promise
  - `warmup()` boots exactly `poolSize` interpreters (verify via `ping` status)

  > **Done (2026-07-17).** Vitest + `npm test` and pool/worker suites already existed from Phases 01–02, so this task consolidated instead of duplicating: extracted shared fixtures into `tests/helpers.ts` (`buildWorkerBundle()` memoized esbuild of the worker bundle, `createPool()`, `withPool(size, fn)` with guaranteed `terminate()`, pool-side `pickleCall`/`unpickle`), renamed `tests/pyodide-pool.test.ts` → `tests/pool.test.ts` (git mv) and refactored it plus `tests/pyodide-worker.test.ts` onto the fixtures. Added the missing named cases: pool-level scalar/list/dict results, and size-1 worker recycling asserting `bootMs === 0` via a ping-backed `warmup()` plus a `sys` marker probe. Also added `tests` to tsconfig `include` (typecheck now covers the suite; fixed two `noUncheckedIndexedAccess` nits in `tests/pyodide-pool-python.test.ts`). All six required behaviors covered; `tests/pool.test.ts` + `tests/pyodide-worker.test.ts` = 33 tests, green twice in a row. `tests/pyodide-scheduler.test.ts` / `tests/pyodide-pool-python.test.ts` were left for the next task, which should migrate their duplicated esbuild `beforeAll` blocks onto `tests/helpers.ts`.

- [x] Write the dask scheduler and package-mirroring test suite in `tests/dask-scheduler.test.ts` (driver Pyodide in the Node main thread, as in `examples/node-dask-demo.ts` — reuse that bootstrapping via a shared fixture in `tests/helpers.ts`):
  - Correctness: for several graph shapes (independent leaves + reduction, a diamond dependency, nested delayed calls, `dask.bag` map/filter/sum), assert `pyodide_pool.compute(...)` equals `dask.compute(..., scheduler='synchronous')`
  - Literal graph values and zero-task graphs resolve without error
  - A task that raises in a worker surfaces the original exception type and traceback in the driver
  - Package mirroring: a numpy-using delayed task returns the correct result; a second numpy task on the same pool completes faster than the first (install replay is idempotent/cached)
  - Concurrency bound: with poolSize 2 and 6 ready tasks, peak in-flight tasks never exceeds 2 (instrument via progress events or worker ping status)

  > **Done (2026-07-17).** Extracted the demo's driver bootstrapping into shared fixtures `bootDriver(pool)` / `bootDaskDriver(pool)` in `tests/helpers.ts` (loadPyodide + register `js_pyodide_pool` + cloudpickle/micropip + write `python/pyodide_pool` into the FS; the dask variant micropip-installs dask from PyPI and occupies all workers once so mirroring cost is paid up front). Renamed `tests/pyodide-scheduler.test.ts` → `tests/dask-scheduler.test.ts` (git mv) onto the fixtures and filled in the named cases: diamond dependency, nested delayed calls, bag map/**filter**/sum, literal + zero-task graphs (asserting **0** bridge submits), strengthened exception test (original `ValueError` re-raised, `RemoteTraceback` cause text contains `ValueError: scheduler boom` + raising frame), numpy mirroring via delayed batches (`second_s < first_s / 2`, plus ping-status proof both workers report numpy loaded), and the concurrency bound — 6 ready 0.4 s tasks on poolSize 2 must land on exactly 2 interpreters (uuid tag persisted in worker `builtins`) with an interval-sweep peak of exactly 2 (≥2 = concurrent dispatch, ≤2 = bounded). Also migrated `tests/pyodide-pool-python.test.ts` onto `bootDriver`/`createPool`, deleting the last duplicated esbuild `beforeAll` (per the previous task's note). `npm test`: 4 files / 50 tests green; typecheck clean.

- [x] Run `npm test` and fix all failures. Iterate on source code and tests until the whole suite is green twice in a row (guard against flaky timing assertions — prefer structural assertions like `bootMs` over wall-clock deltas where possible). Do not weaken correctness assertions to make tests pass; fix the underlying code.

  > **Done (2026-07-17).** `npm test` ran green twice consecutively with zero failures — 4 files / 50 tests each run (~15.1 s per run) — and `npm run typecheck` is clean, so no source or test fixes were needed (the two prior tasks left the suite stable). Audited the suite for flaky timing assertions per the guard: the only timing-based expectations are structural counters (`bootMs > 0` on cold boot, `bootMs === 0` for worker recycling, `execMs >= 0`) and the two package-mirroring cache checks (`second_s < first_s / 2` in `tests/dask-scheduler.test.ts` and `tests/pyodide-pool-python.test.ts`), which compare a multi-second cold numpy install against a near-instant cached replay at a generous 2x margin and are each corroborated by a structural ping-status assertion that both workers report numpy loaded — nothing fragile, nothing changed.

- [x] Build the benchmark harness `bench/run-bench.ts` wired to `npm run bench` (plain script with `performance.now()` timing and warmup runs — keep it dependency-light rather than adding a bench framework):
  - Workloads: (a) pure-Python prime counting (CPU-bound, from Phase 01), (b) Monte Carlo pi estimation (random-heavy pure Python), (c) numpy batch matmul (tests mirrored-package + serialization overhead), (d) a dask.delayed reduction graph via the Phase 02 scheduler
  - Matrix: serial baseline (warmed size-1 pool) vs pool sizes 2, 4, and 8 (cap at `os.availableParallelism()`), fixed total work per workload, 3 repetitions reporting median
  - Also measure and report overheads separately: per-worker Pyodide boot time, per-task round-trip latency for a no-op task, and cloudpickle payload round-trip time for a 1 MB numpy array
  - The script writes machine-readable results to `bench/results/node-<iso-date>.json` and regenerates the human report (next task) from that JSON

  > **Done (2026-07-17).** Three dependency-light files: `bench/schema.ts` (shared result-JSON types + `median`/`fmtMs`), `bench/run-bench.ts` (harness + CLI), `bench/report.ts` (pure JSON→markdown generator with front matter, wiki-links, per-workload speedup/efficiency tables, overheads, and data-driven analysis prose — so the next task's report regenerates from the JSON). Reuses `tests/helpers.ts` fixtures (`createPool`/`buildWorkerBundle`/`bootDriver`) instead of duplicating boot code. Workloads (a)/(b) go through `pool.map()`; (c) goes through driver-side `_wp.submit` batches so package mirroring + cloudpickle result payloads (full 256×256 float64 matrices) are genuinely exercised; (d) uses `pyodide_pool.compute(_bench_total, pool=_wp)` — ONE driver Pyodide boots for the whole bench and each pool is handed over via `globals.set('js_bench_pool', pool)` + `pyodide_pool.WorkerPool(js_bench_pool)`, with an untimed all-worker mirroring warm-up per pool. Matrix sizes `[1,2,4,8]` capped at `availableParallelism()` (16 here); per cell 1 untimed warmup + 3 timed reps; every cell's verification value must match across reps AND pool sizes (primes/dask also against π(2·10⁶)=148933, π estimate within 0.01). Overheads: per-worker `bootMs` from every cold warmup, no-op `runPython('None')` RTT (×10), 1 MiB float64 cloudpickle echo (×5, integrity-checked). npm scripts: `bench` (full → `bench/results/node-<date>.json` + `docs/benchmarks/node-benchmarks.md`; report skipped + exit 1 on verification failure), `bench:report` (regenerate from latest JSON), `bench -- --smoke` (tiny self-test matrix → gitignored `bench/results/node-smoke*`, ~8 s). Verified: typecheck clean, smoke run green twice (speedups 1.86×/1.75× on 2 workers for the pure-Python workloads even at smoke sizes), `--report-only` errors cleanly without JSON, `npm test` still 4 files / 50 tests green. Full-mode workload sizes calibrated from measured smoke throughput (~5 M π-samples/s/worker → 16 M samples; matmul n=256 k=20). `tests` include in tsconfig extended with `bench`.

- [x] Run `npm run bench` to completion and generate `docs/benchmarks/node-benchmarks.md` from the results JSON: front matter (`type: report`, `title: Node.js Benchmarks`, `created` date, tags `[benchmark, node, pyodide]`), wiki-links `[[phase-02-results]]` and `[[browser-benchmarks]]` (forward link, file comes in Phase 04), a table per workload (rows: serial/2/4/8 workers; columns: median wall-clock, speedup, efficiency), the overhead measurements, hardware/Node/Pyodide version context, and a short prose analysis of where speedup saturates and why (boot amortization, serialization overhead, core count).

  > **Done (2026-07-17).** `npm run bench` ran to completion twice (~150 s each, zero verification failures across the 16-cell matrix) — results in `bench/results/node-2026-07-17.json`, report regenerated to `docs/benchmarks/node-benchmarks.md`. The report generator from the previous task already emitted every required element, so the report was verified against this spec item-by-item rather than rebuilt: front matter (`type: report`, title, created 2026-07-17, tags `[benchmark, node, pyodide]`), `related:` wiki-links `[[phase-02-results]]` (resolves to `docs/notes/phase-02-results.md`) + `[[browser-benchmarks]]` (Phase 04 forward link), four per-workload serial/2/4/8 tables with median/speedup/efficiency columns, the three overhead rows, environment table, and data-driven analysis prose covering boot amortization (~1.1 s/worker), serialization floors (0.7 ms no-op, ~10.6 ms per MiB cloudpickle), and core count. One real fix: the hardware row read `CPU | unknown` because `os.cpus()` reports no model on this WSL2 arm64 box — added a `/proc/cpuinfo` fallback (`model name`, else ARM implementer/part registers) to `bench/run-bench.ts`, giving `Qualcomm ARM64 (part 0x002)`. Committed numbers (8 workers): primes 4.76×, Monte Carlo π 6.43×, numpy matmul 4.13×, dask.delayed 4.81×; all four workloads scale monotonically with no taper below the 1.4× marginal threshold, efficiency 52–80% at 8 workers. `npm test` still 4 files / 50 tests green; typecheck clean.
